# Optional local inference dependencies.
# Install only when this WSL environment must run YOLO prediction locally.
# If torch is already installed, prefer:
#   pip install ultralytics opencv-python --no-deps
ultralytics
opencv-python
