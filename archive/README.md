# 历史归档

本目录只保存不再参与当前训练与测试的可复现资产。

- `splits_legacy_random_560_95_95/`：随机逐帧旧划分及其 IR/SAR、旧类/新增类复核子集。

当前唯一活动数据划分位于 `splits/`。`models/generations.json` 和多批次压力配置中指向本目录的路径只用于解释或复现历史模型证据，不能作为当前严格 3+1 性能结果。
