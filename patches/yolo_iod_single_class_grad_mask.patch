diff --git a/yolo_world/engine/runner/gps_loops.py b/yolo_world/engine/runner/gps_loops.py
--- a/yolo_world/engine/runner/gps_loops.py
+++ b/yolo_world/engine/runner/gps_loops.py
@@ -315,8 +315,8 @@ class EpochBasedTrainGPSLoop(BaseLoop):
             for param_name, param in self.runner.model.named_parameters():

                 if not param.requires_grad:
                     continue
-                # if param_name not in self.grad_mask:
-                #     continue
+                if param_name not in self.grad_mask:
+                    continue
                 if len(param.shape) > 2:
                     with torch.no_grad():
                         ori_param = self.ori_model[param_name]

diff --git a/yolo_world/engine/runner/runner.py b/yolo_world/engine/runner/runner.py
--- a/yolo_world/engine/runner/runner.py
+++ b/yolo_world/engine/runner/runner.py
@@ -1773,7 +1773,14 @@ class Runner:
         # Maybe compile the model according to options in self.cfg.compile
         # This must be called **AFTER** model has been wrapped.
         self._maybe_compile('train_step')

         # cal grand
+        train_iter = self._train_loop.iter  # type: ignore
         self.train_loop.run_cal_grad()  # type: ignore
+        # GPS importance collection consumes optimizer accumulation counts and
+        # resets the loop iteration. Restore both before real training/resume.
+        self._train_loop._iter = train_iter  # type: ignore
+        self.optim_wrapper.initialize_count_status(
+            self.model, train_iter,
+            self._train_loop.max_iters)  # type: ignore

         # train
         self.load_checkpoint(self._load_from)
